/**
 * ON-CHAIN COMPONENTS - EXPORT INDEX
 * Reusable UI components for displaying on-chain whale data
 */

export { WhaleActivityBadge } from './WhaleActivityBadge';

// Export hooks from hooks directory
export {
  useWhaleActivity,
  useWhaleActivityMultiple,
  useOnChainOverview,
  useWhaleNotifications,
} from '@/hooks/useWhaleActivity';
