# Database Service Dependencies

# Core
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0

# Database
psycopg2-binary>=2.9.0  # PostgreSQL / TimescaleDB adapter

# Shared utilities dependencies (already in shared/requirements.txt)
redis>=5.0.0
prometheus-client>=0.19.0
