# Shared Utilities Dependencies
# Install with: pip install -r requirements.txt

# Core
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Caching
redis>=5.0.0

# Metrics & Monitoring
prometheus-client>=0.19.0

# Data Processing (optional, used by some services)
numpy>=1.24.0

# Logging (built-in, no extra package needed)

# Note: Individual services may have additional requirements
# Install those separately in each service's venv
